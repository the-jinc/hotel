export interface MeetingAndEvent {
  id: string;
  name: string;
  description: string;
  images: string[];
  capacity: number;
  eventType: string;
  // Add other meeting and event properties as needed
}
